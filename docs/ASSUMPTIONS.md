# 假设登记表：dsh-model-health

> 配套 `SPEC.md`。每条假设标注风险等级；高风险假设必须在建造前完成验证并回填结论。
> 结论列取值：`✅ 已验证` / `❌ 已证伪` / `待验证`。验证坐标指向可复核的源码行或实测命令。

| 编号 | 假设 | 风险 | 验证方法 | 结论 |
| --- | --- | --- | --- | --- |
| A1 | `ctx.get("settings").get("llm-pi-ai").providers` 是当前进程内可读的配置源，且插件卸载后命名空间才注销 | [中] | 读 `@deepseek-ai/dsh-settings` 类型定义 `SettingsProvider.get(ns)`；对照 `dsh-llm-pi-ai` 的命名空间常量 `NS` | ✅ 已验证（`dsh-settings/lib/types/index.d.ts:243`，`dsh-llm-pi-ai/lib/index.js` 注册 `llm-pi-ai`） |
| A2 | `ctx.get("credentials").resolve(credentialRef(ref))` 能解析出本机 12 个 `apiKeyEnv` 的值 | [高] | 实测三个环境作用域（进程 / User / Machine）均无这些变量；值位于 `C:\Users\pc\.dsh\.credentials.yaml` 的 `refs:` 段；宿主 `dsh-llm-pi-ai` 走同一路径 | ✅ 已验证（`dsh-llm-pi-ai/lib/index.js:2595-2600`；`.credentials.yaml` 的 refs 段含全部 12 个引用名） |
| A3 | 三协议 URL 拼接式为 `new URL(baseURL + (baseURL.endsWith('/') && path.startsWith('/') ? path.slice(1) : path))`，且 openai 与 anthropic 两个官方 SDK 实现逐字相同 | [高] | 读 `openai/client.mjs:282` 与 `@anthropic-ai/sdk/client.mjs:390` | ✅ 已验证（两处代码逐字一致） |
| A4 | openai 系 baseURL 必须自带 `/v1`，anthropic 系必须不带 `/v1` | [高] | 读两个 SDK 的默认 baseURL：`openai/client.mjs:134` 为 `https://api.openai.com/v1`；`@anthropic-ai/sdk/client.mjs:88` 为 `https://api.anthropic.com`（路径 `resources/messages/messages.mjs` 为 `/v1/messages`） | ✅ 已验证 |
| A5 | 本机配置归一化后为 6 个供应商组、7 个 `(baseURL, api)` 分组、12 条路由 | [中] | 读 `C:\Users\pc\.dsh\settings.yaml` 的 `llm-pi-ai.providers` 并做尾斜杠归一化统计（settings.yaml 于 2026-09-17 被用户修改过，`xcmapi-guomo` 的 baseURL 已补 `/v1`） | ✅ 已验证（清单见 `SPEC.md` §5.3） |
| A6 | 第四行「路由」的出现条件为「同一分组内声明了同一 `modelId` 的路由数 > 1」，本机触发点是 xcmapi 的 `gpt-5.6-sol`（2 条）与 aihub 的 `gpt-5.6-sol`/`gpt-5.6-terra`（3 条） | [中] | 对 `settings.yaml` 按 `(baseURL 归一化, api, modelId)` 聚合路由键计数 | ✅ 已验证 |
| A7 | openai 流式必须发 `stream_options: {include_usage: true}` 才能拿到 usage | [高] | 读 `pi-ai/dist/api/openai-completions.js:594-596` | ✅ 已验证 |
| A8 | anthropic 流式的 usage 分两处（`input_tokens` 在 `message_start`、`output_tokens` 在 `message_delta`），只取一次会丢 `input_tokens` | [高] | 读 `pi-ai/dist/api/anthropic-messages.js:409-425` 与 `:556-575`（源码注释明写保留 `message_start` 的 `input_tokens`） | ✅ 已验证 |
| A9 | anthropic 流式终止符为 `message_stop`（不是 `[DONE]`），缺失时上游 SDK 会抛错 | [中] | 读 `pi-ai/dist/api/anthropic-messages.js:316,327` | ✅ 已验证 |
| A10 | 宿主 `webServer` 服务支持 `register({kind:"exact", path, handler})` 且返回 disposer；`ctx.get("webServer")` 可惰性获取 | [中] | 读 `dsh-host-webserver/lib/types/index.d.ts:31-38,90`；对照 `dsh-concurrency-guard/lib/api.js:146-241` 的既有用法 | ✅ 已验证 |
| A11 | 客户端经 `ctx.slots.inject("conversation.view", …)` 注册的页签会出现在会话视图页签条上 | [中] | 读 `dsh-client-ui-conversation/lib/types/client/contract/slots.d.ts:156-161`（`conversation.view` 为 list 槽，scope=session）；对照 guard 的面板注册代码 | ✅ 已验证 |
| A12 | 客户端 bundle 可用 `window.__ModuleLoader__.load({id, factory})` 手写，零构建链，`require("react")` 由 shell seed 提供 | [中] | 对照 `dsh-concurrency-guard/lib/client.js:17-25` 与 `package.json` 的 `dsh.client.inject` | ✅ 已验证 |
| A13 | 4 MiB 响应体上限与「先查 content-length 再累计校验」两段式是可照抄的宿主先例 | [低] | 读 `dsh-llm-pi-ai/lib/index.js:2142,2167-2200` | ✅ 已验证 |
| A14 | 本机不存在 `openai-responses` 类型的已配置路由，该协议只能以 mock 覆盖 | [中] | 统计 `settings.yaml` 中 `api: openai-responses` 的条数 | ✅ 已验证（条数为 0；`SPEC.md` AC17 已如实标注） |
| A15 | 面板展示的 `previewUrl` 与真实请求 URL 恒等的前提是两者共用同一 `buildRequest()` 函数 | [中] | 设计约束，由 AC4 在验收脚本中断言；实现侧以「客户端禁止自行拼接」作为配套约束 | ✅ 已验证（约束写入 `SPEC.md` §11.1 与 AC4） |
| A16 | 真实端点冒烟会消耗上游额度，需 captain 决定执行时机 | [低] | 由 captain 决策；冒烟失败不自动重试 | 待验证（人工决策项，非技术假设） |
| A17 | 计数口径的唯一性：本机「供应商组 = 6 / API 类型按钮 = 7 / 路由 = 12」只在「仅扫 `llm-pi-ai.providers` + 只去尾斜杠归一化」下成立；若误扫全配置段并按**原始** baseURL 去重会得到 7 组 | [高] | qa-verifier 独立脚本 `D:\Company\dsh-plugin\_qa-ground-truth.mjs` 实测复现：`supplierGroups=6 apiButtons=7 routeButtons=12`，并输出「PROVENANCE OF 7」小节，定位到 `describe-image.baseURL=https://happycodeai.com`（非 LLM provider）与 `llm-pi-ai.happycodeai.baseURL=https://happycodeai.com/` 仅差尾斜杠 | ✅ 已验证（两个「7」均为真值但含义不同，已在 SPEC §5.1/§5.3 冻结扫描范围并设 AC2 反越界用例） |
| A18 | 双 `/v1` 配置怪癖可真实复现：`aimax66`（anthropic + `http://ai.max66.xyz/v1`）终址为 `/v1/v1/messages`；`happycodeai`（anthropic + 无 `/v1`）为单 `/v1` | [中] | 按 §6.1 拼接式手工推演 + qa-verifier ground-truth 脚本确认两条 provider 的 baseURL 与 api 组合 | ✅ 已验证（SPEC §6.1 列两种形态为正式用例，AC5b 断言不得静默去重） |
| A19 | 「密钥未配置」UI 态在本机真实配置下不可触发（12/12 密钥已配置，环境作用域 0/12） | [中] | qa-verifier 脚本实测：`refs` 命中 12/12、`process.env` 命中 0/12 | ✅ 已验证（SPEC AC9 已标注该类 code 一律用 mock/构造数据判定） |
| A20 | 组数/路由数必须运行时动态推导，不得硬编码；本机期望值（provider 12 / 供应商组 6 / API 按钮 7 / 路由 12 / aihub 3 / xcmapi 4 / ai.max66.xyz/v1 两种 api / responses 0）随用户改 `settings.yaml` 而变化 | [高] | SPEC §5.3 已列「期望值对照基准」表并写明动态性硬约束（R2b）；AC2 设反硬编码用例：构造 3 组假配置须输出 3 组 | ✅ 已验证（口径与期望值均已写死，并附反硬编码验收用例） |
| A21 | Node 24 起 `--test` 后的位置参数按「测试文件路径」解析，不再当目录递归根；故 `node --test tests/` 会 exit 1，而 `node --test`（无参）与 `npm test` 正常 | [中] | 本规格编写时在 Node **v24.18.0** / Windows 独立复跑：`node --test tests/` → exit 1；`node --test` → exit 0；`node --test tests/probe.test.mjs` → exit 0；`node --test "tests/**/*.test.mjs"` → exit 0。captain 与 dev-host 亦独立复现 | ✅ 已验证（SPEC §15.1b 写死唯一验收命令 `npm test`，AC21 禁止三种目录形式，并声明不得据此判实现缺陷） |
| A22 | captain 报告称 `node --test "tests/*"` 不可用，与本机实测不一致（我复跑得 exit 0） | [低] | 独立复跑对照；差异不影响结论（唯一验收命令仍为 `npm test`，glob 形式一律不依赖） | ✅ 已验证（差异已在 SPEC §15.1b 末以注记如实记录，不掩盖） |
| A23 | 「零用例假绿」真实存在：测试文件零匹配时命令绿着通过 0 个用例 | [高] | 本规格编写时实测：空 `tests/` 目录下 `node --test` 输出 `ℹ tests 0` / **exit=0**；`node --test "tests/**/*.test.mjs"` 同样 `tests 0` / exit=0。**换命令不能消除假绿** | ✅ 已验证（SPEC §15.1/§15.4 加零用例守卫 + **AC22**：必须自报用例数且 > 0，`tests 0` 判未验证） |
| A24 | 宿主凭据解析是**三元**语义而非链式兜底：`credentials` 服务存在但 `resolve()` 返回 `undefined` 时**不**再查环境快照；命中后还须过 `assertUsableApiKey()` | [中] | 读 `dsh-llm-pi-ai/lib/index.js:2596-2599`：`credentials !== void 0 ? (await credentials.resolve(ref))?.value : launchEnvironmentOf(ctx).get(ref)?.value`，命中后 `return assertUsableApiKey(hit, "llm-pi-ai", ref)`（`assertUsableApiKey` 自 `@deepseek-ai/dsh-llm` 导入） | ✅ 已验证（SPEC §8.1 已补两条：三元语义不得写成 `a \|\| b`；命中后须过可发送性校验） |
| A25 | 第四行「路由」的渲染判据在 captain 消息间反复变动（先后出现分组作用域 → 模型作用域 → 分组作用域），一度使规格与 dev-client 实现、ui-spec 三方不一致 | [高] | captain 2026-09-17 **最终裁决：撤回「模型作用域」仲裁，恢复分组作用域**。依据=**任务书原话**「同 baseURL+api 下**多条路由**时第四行才出现」→ 分组口径；captain 承认此前用自己后来的 UX 推理推翻了用户写在任务书里的明确条件，是其错误。旁证：dev-client 实现（`row4Visible(group) { return Number(group.routeCount) > 1; }`）与 ui-designer 的 ui-spec（`groups[g].routeCount === 1` → 整行 DOM 不存在）**本来都是分组口径**，只有 SPEC.md 被错误指令改坏 | ✅ 已验证（SPEC v1.0.3 已按最终裁决定稿：渲染条件 `groups[g].routeCount > 1`、候选集按 `models[m].routeKeys` 收窄、**候选数为 1 时仍然渲染**；「模型作用域」字样命中数 = **0**。三方口径现已一致） |
| A26 | 密钥可发送性校验的字符类下界是 `\x21`（`!`）而非 `\x20`（空格）：空格不在合法集内（trim 只处理首尾，内部空格非法） | [中] | 读宿主 `dsh-llm/lib/index.js:472` `LEGAL_API_KEY = /^[\x21-\x7E]+$/` 与 `:484-496` `normalizeApiKey`（先 trim → 空则 `empty` → 不匹配则 `illegalCharacters`）；比对实现 `lib/index.js:433`、`lib/probe.js:194` 均用 `\x21` | ✅ 已验证（SPEC 原写 `[\x20-\x7E]` 系**规格单方笔误**，已按 CHANGE-REQUEST-001 修正为 `\x21`；实现本就正确，无需改代码） |
| A27 | reviewer 预审 F1：§6.1 表格 `openai-responses` 行的示例曾是**假想值**（写成 `https://aihub.dog/v1 → .../responses`，但 aihub 实为 `openai-completions`，本机 0 个 responses 路由），易被 dev-host 误读为真实配置 | [低] | 核对 §5.3 协议覆盖度表（`openai-responses` provider 数 = 0）与 §5.3 明细表（aihub.dog/v1 的 api 为 `openai-completions`） | ✅ 已验证（SPEC v1.0.1 已把该行标注为「**示意（本机无 responses 路由，见 §5.3 / AC17）**」，并改为纯规则说明「任何自带 `/v1` 的 baseURL → `<base>/responses`」；列名由「本机示例」改为「示例」，另两行显式标「本机」） |
| A28 | reviewer 预审 F2：AC2 曾未在其**判定方式**内显式钉死扫描范围，t16/t17 若只读 AC2 仍可能按「全配置段原始 baseURL 去重 = 7」判定 | [低] | 核对 AC2 原文与 §5.1 扫描范围条款；范围不同确会得到 6 或 7（`describe-image.baseURL` 与 `llm-pi-ai.happycodeai.baseURL` 仅差尾斜杠） | ✅ 已验证（SPEC v1.0.1 已在 AC2 **判定方式**补「扫描范围 = 仅 `llm-pi-ai.providers`；不包含 `describe-image`、`llm-deepseek` 等命名空间」；准则格原有的术语澄清保留） |
| A29 | 四层选择器**不是自由笛卡尔积**：xcmapi 分组同 baseURL+同 api 下 4 条路由模型集互不相同，模型行去重 6 × 路由行 4 = 24 自由组合中**仅 7 个真实存在**；全量 30 个「模型 × 路由」组合中 **17 个（57%）解析不到 provider** | [高] | qa-verifier 独立脚本 `_qa-cartesian.mjs` 枚举实测（我复跑确认逐行一致）：`glm-5.3-flash`/`deepseek-v4.1-flash`/`...-expires-on-0910` 各 1/4；`deepseek-v4-flash`/`k3` 各 1/4；`gpt-5.6-sol` 2/4 | ✅ 已验证（SPEC v1.0.2 新增 **§7.6** 写死**级联过滤**（决策 (a)）+ **AC23** 不变式断言；`aihub` 6/6 全有效的陷阱已显式标注为「必须用 xcmapi 验证」） |
| A30 | 路由去重**只可在同一 api 内**进行，绝不跨 api：distinct apiKeyEnv = **11** 但路由槽位 = **12**，因 `AI_MAX66_API_KEY` 被 `ai.max66.xyz/v1` 的 anthropic 与 openai 两条路由共用 | [中] | qa-verifier 脚本 `_qa-crosscheck.mjs` 实测（我复跑确认）：`AI_MAX66_API_KEY: 2 slots -> anthropic-messages@ai.max66.xyz/v1 \| openai-completions@ai.max66.xyz/v1`；备份配置无此共享 | ✅ 已验证（SPEC v1.0.2 §7.1 步骤 3 写入硬约束「路由去重仅同 api 内，绝不跨 api」，并附 11 vs 12 实证） |
| A31 | 模型行规模口径：按 modelId **去重**显示（xcmapi = **6**），不是按路由累计 raw（同组 = **7**，因 `gpt-5.6-sol` 在 api-tcvps-006/008 各出现一次） | [中] | qa-verifier 脚本实测：模型行去重 6 / raw 7；reviewer 独立复核确认去重 6 为正确口径 | ✅ 已验证（SPEC v1.0.2 §7.1 步骤 4 写死「验收取去重值 6；raw 计数不得作为断言口径」，并与级联语义绑定） |
| A32 | 基线 6/7/12 是**快照**而非常量：备份 `settings.yaml.bak.20260913-234803` 实测 providers=10 / 6 组 / apiButtons=**6** / routes=**10**（组数 6 恰好相同，但 apiButtons 与 routes 不同） | [中] | qa-verifier 脚本 `_qa-crosscheck.mjs` 对比当前 vs 备份（我复跑确认）；备份多 `https://3c.al`(A3CAL_API_KEY)、当前多 `http://ai.max66.xyz/v1`（双协议） | ✅ 已验证（SPEC v1.0.2 维持既有「动态性硬约束」R2b 与 AC2 反硬编码用例；本次新增 AC23 亦以「枚举推导」而非固定数字断言，与快照口径一致） |

## 高风险假设汇总

A2、A3、A4、A7、A8 为高风险假设，均已通过读源码 / 实测完成验证并在结论列回填 ✅。剩余未决项仅 A16（人工决策项，不影响建造启动）。
