# 变更单：四层选择器级联语义写死（禁止自由笛卡尔积）+ 模型行去重口径 + 路由不得跨 api 去重

> 本文件由 keel 生成。规格冻结后的一切新增与修改都必须先填本单，
> 批准后更新规格书，再继续实现。

| 字段 | 内容 |
| --- | --- |
| 请求人 | qa-verifier（独立枚举实测）＋ reviewer（独立方法复核，结论一致） |
| 日期 | 2026-09-17 |
| 原规格位置 | §7.2 四行按钮数据流 / §7.3 选择状态保持 / §7.1 分组算法 / §14 验收标准；关联 R7 / R6 / AC14 |
| 变更内容 | 新增/收紧了四层选择器的**级联语义**（选 (a) 级联过滤），共 5 处：① §7.2 行表④ 与新增 §7.6「四行不是自由笛卡尔积」：明确第四行候选集恒为 models[m].routeKeys 收窄结果，**禁止**四行各自独立维护选中值；② §7.3 补「无效组合不可达」证明与防御性校验；③ §7.1 步骤 4 补「模型行按 id 去重（本机 xcmapi 去重后 6，raw 累计 7）」口径；④ §7.1 步骤 3 补「路由去重仅在同一 api 内，**绝不跨 api 去重**」（distinct apiKeyEnv=11 但路由槽位=12，因 AI_MAX66_API_KEY 被 anthropic/openai 两路由共用）；⑤ 新增 **AC23** 级联验收（含 xcmapi 17/30 无效组合不可达断言）。 |
| 理由 | qa-verifier 用独立脚本 _qa-cartesian.mjs 枚举实测（我复跑确认）：xcmapi 分组同 baseURL+同 api 下 4 条 provider 各自模型互不相同，模型行去重后 6 个、路由行 4 个，自由笛卡尔积 24 组合中**仅 7 个真实存在**；全量 30 个 model×route 组合中 **17 个（57%）解析不到任何 provider**。关键陷阱：aihub 分组 6/6 全有效，**只测 aihub 会误判无问题**。若实现按「四行各自独立维护选中值」最自然写法，用户可选 glm-5.3-flash + API_TCVPS_006_API_KEY 这种不存在的组合，导致预览拼不出目标、发出语义错误请求、且错误人话化给出**误导性归因**（用户以为供应商故障）。原 §7.2/§7.4 只描述候选集来源，**未明确禁止自由组合**，也未定义无效组合的 UI 表现，属设计决策缺口。 |
| 影响范围 | 需求：R7 措辞收紧为「级联过滤」；新增 R7b（禁止自由笛卡尔积）。验收标准：新增 AC23（级联与无效组合不可达）；AC14 补级联断言。**实现：零改动**——dev-host 现有实现 lib/providers.js:141-142（routeKeys 归属）与 lib/client.js:139-146（row4Candidates 按 model.routeKeys 收窄）**已经是级联语义**，本条仅把实现事实写进规格，消除 t15/t16/t17 的判定歧义。工作量：纯规格文本修订。 |
| 决策 | 批准 |
| 推迟到 | 无（即时生效，无推迟项） |
| 批准人 | captain（授权我按 reviewer/qa 预审情报做轻量变更单；本条为设计决策必须写死项） |